import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  allowedDevOrigins: ['trusting-hydrogen-impotency.ngrok-free.dev'],
}
export default nextConfig;
